# Database
DATABASE_URL="file:./dev.db"

# Chain: "base" or "base-sepolia"
NEXT_PUBLIC_CHAIN="base-sepolia"

# Contract address (deploy first, then set)
NEXT_PUBLIC_CONTRACT_ADDRESS="0x..."

# Session secret (generate: openssl rand -hex 32)
SESSION_SECRET="your-secret-at-least-32-characters-long"

# Server salt for deterministic RNG (generate: openssl rand -hex 32)
SERVER_SALT="your-server-salt-hex-string"

# App URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Private key for anchoring match results (server-side EIP-712 signer)
ANCHOR_SIGNER_PRIVATE_KEY="0x..."
